// Authentication is now handled by the backend at POST /v1/auth/login
// Tokens are stored in localStorage (client-side) and auth_token cookie (server validation)
// See app/api/auth/login/route.ts and lib/api-client.ts for implementation details
